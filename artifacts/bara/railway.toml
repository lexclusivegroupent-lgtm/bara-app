[build]
buildCommand = "pnpm install && cd artifacts/bara && npx expo export --platform web"

[deploy]
startCommand = "npx serve artifacts/bara/dist --single"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
