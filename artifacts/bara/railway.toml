[build]
buildCommand = "npm install -g pnpm@latest && pnpm install --no-frozen-lockfile && node scripts/build.js"

[deploy]
startCommand = "node server/serve.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
